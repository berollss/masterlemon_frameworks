import { createStyles } from '@material-ui/core/styles';

export const membersPageStyles = () =>
  createStyles({
    searcherContainer: {
      display: 'flex',
      marginBottom: 16,
    },
    textField: {
      marginRight: 8,
    },
    title: {
      marginBottom: 32,
    },
  });
